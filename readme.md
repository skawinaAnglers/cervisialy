# React Native Boilerplate

## Install packages:

`yarn`

## Scripts:

### Start development

`yarn dev:tailwind`
`yarn start`

### ESLint

`yarn lint`

### Check formatting with Prettier

`yarn format`

### Force reformat:

`yarn format:write`
